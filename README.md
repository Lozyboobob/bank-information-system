# bank-informationn-system

--Dependency configuration--

All the libraries are managed by gradle.
In the command line, access the root of the project and find the build.gradle file.

Type: 
gradle build
gradle eclipse

The first command is to build the whole gradle project, and the second one is to convert it to an eclipse project.
After that, import the project to your eclipse